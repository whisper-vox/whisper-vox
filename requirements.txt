# Whisper Vox - Python dependencies.
# Set up (Windows):
#   py -3.11 -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements.txt
# Set up (macOS):
#   python3.11 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt

# UI
pywebview==6.2.1
pystray==0.19.5
Pillow==12.2.0

# macOS integration: menu-bar item, clipboard, window geometry, permissions.
# pywebview already pulls in Cocoa/Quartz/WebKit on darwin; they are pinned here
# too so a CI build cannot drift. AVFoundation is NOT pulled in by anything - it
# is needed only to read the microphone permission state.
pyobjc-framework-Cocoa==12.2.1; sys_platform == 'darwin'
pyobjc-framework-Quartz==12.2.1; sys_platform == 'darwin'
pyobjc-framework-WebKit==12.2.1; sys_platform == 'darwin'
pyobjc-framework-AVFoundation==12.2.1; sys_platform == 'darwin'

# Windows notifications: a real toast, which Windows files in its notification
# history and which can carry an "Update now" button. The tray balloon it
# replaces could do neither. Windows-only wheels, hence the marker.
winrt-runtime==3.2.1; sys_platform == 'win32'
winrt-Windows.Foundation==3.2.1; sys_platform == 'win32'
winrt-Windows.Data.Xml.Dom==3.2.1; sys_platform == 'win32'
winrt-Windows.UI.Notifications==3.2.1; sys_platform == 'win32'

# Audio capture + transcription
openai==2.41.0
sounddevice==0.4.6
soundfile==0.12.1
numpy==1.24.3
pynput==1.7.6
PyYAML==6.0.1

# Build tool
pyinstaller==6.20.0
